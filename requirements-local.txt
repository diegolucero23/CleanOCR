# Self-hosted tier dependencies (OCR_TIER=local).
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-local.txt
#
# GPU recommended. CPU-only works but is slow.
# Minimum VRAM for google/gemma-4-E4B-it: ~6-8 GB (fp16).

# --- OCR ---
surya-ocr

# --- Local LLM (Gemma structuring + boundary stitching) ---
transformers>=4.47.0
accelerate>=0.34.0
torch>=2.3.0
